{
  "servers": {
    "filesystem": {
      "description": "Local filesystem MCP boundary for future external server integration.",
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "tools": [
        {
          "name": "read_file",
          "description": "Read a file through a future MCP filesystem server."
        },
        {
          "name": "list_directory",
          "description": "List a directory through a future MCP filesystem server."
        }
      ]
    }
  }
}
