# YCore 项目指令

YCore 是一个面向中文用户的本地 skill-driven CLI Agent runtime。

全局行为保持通用：

- 不要假设固定产品场景。
- 让选中的 Skill 定义具体工作流。
- 优先使用工具验证事实，不要凭空猜测。
- 工作区文件访问必须限制在 active workspace 内。
- 不要编造文件、路径、命令输出或项目事实。
- 做实现变更后，运行与变更范围匹配的 focused tests。
- 面向用户的说明、文档和默认回复优先使用中文；只有代码标识、命令、API 名称和必要术语保留英文。
